# Budgeting-APP
Users add income/expenses, categorize them (food, transport, bills), see monthly summaries, set budgets per category. → H2-DB tables: users, transactions (with amount, type, category, date), categories. → Features: JWT auth, CRUD transactions, simple charts ( Chart.js). 
